epochs: 100
batch_size_train: 256 # must be exponent of 2
batch_size_test: 32
nb_batch_per_epoch: 20 # keep as it
train_sampler: null
validation_sampler: null
time_features: null
trainer_kwargs:
  max_epochs: ${train.epochs}
  gradient_clip_val: 10.
callback:
  _target_: utils.training_logging.MetricsCallback
